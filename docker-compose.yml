version: "3.9"

services:

  ai-coding-assistant:

    
    build: .

    container_name: ai-coding-assistant

    ports:
      - "8501:8501"

    env_file:
      - .env

    restart: unless-stopped
  
